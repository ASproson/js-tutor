// You will be passed a string. If the string already ends with a number, increment that number by 1
// If the string does not end with a number, the number 1 should be appended to the string

const incrementString = (str) => {
    
};

  module.exports = incrementString;