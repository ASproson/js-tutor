// Using the template below, create a basic JavaScript object that contains a name, and an age

const basicObject = {
    
}

console.table(basicObject)

// Now change the name and age within the object



console.table(basicObject)

// Now add another property to the object



console.table(basicObject)


