// Create a function that returns a shallow copy of the provided array
// Bonus: store the copy of the array inside of the function as a variable, before returning that variable, add something to your copy array. Has the original array changed?

let providedArray = [1, 2, 3, 4, 5]



console.log(arrayShallowCopy(providedArray))